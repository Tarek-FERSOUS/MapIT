NODE_ENV=production

# Public URL used by client code when needed
NEXT_PUBLIC_API_URL=https://mapit.company.com/api/backend

# Internal API endpoint used by Next.js route handlers on WEB VM
# Point this to API VM private address or internal DNS
API_URL=http://10.10.10.20:3002
